admin page
	- Import Events by JSON (asking chatgpt to generate list of events based on newspaper ad or facebook events i screenshot), all event created by import will be default to inactive status. 
	- Add Photo Cards (a standalone photo upload page, easier to design and develop, one upload, one photo card ) 
	- Manage Existing Cards (contains two tabs, one is event, one is photo, this is the place we can edit all the existing data
	
user page
	- 
	

admin page (keep in view) for now because i dont think i will use it)
	- create card
	  + type: photo/event (dont show the rest of the fields until this is selected, if changed, reset all fields)
	  + title: text field
	  + description: text field
	  + location: text fieldevents
	  + photos: button to add photo, if already added, shows a thumbnail nearby
	  + organizer: text field ==> only for events
	  + start date (date only): date only field  ==> only for events
	  + end date (date only): date only field ==> only for events
	  + source url ==> only for events
	  + source screenshot url ==> only for events
	  + source remarks ==> only for 
	  + event schedules: a button "refresh" to check on start and end date, if both are filled, auto generated schedule for each day, with start and end time leave it empty. 
		* event program: under each event schedule, you could add event program to it
			
	  
	  
	  